---
description: Run the full E2E debugging flow (Server + App + Log Monitor)
---

// turbo-all

1. Run the debug flow script
   `./scripts/run-e2e-debug-flow.sh`

2. Interact with the app
   The script passes ADB broadcasts to switch the network mode.
