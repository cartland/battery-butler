name: Verify Release Build

# Builds a signed release bundle on every green CI on main.
# This proves the release pipeline works WITHOUT deploying.
# Artifacts are uploaded for 30 days if needed for manual deployment.
#
# This does NOT:
# - Create tags
# - Upload to Play Store
# - Deploy anything

on:
  workflow_run:
    workflows: ["Battery Butler CI"]
    types: [completed]
    branches: [main]

env:
  ORG_GRADLE_PROJECT_PRODUCTION_SERVER_URL: ${{ secrets.PRODUCTION_SERVER_URL }}
  ORG_GRADLE_PROJECT_DEV_SERVER_URL: ${{ secrets.DEV_SERVER_URL }}

permissions:
  contents: read

jobs:
  verify-release-build:
    # Only run if CI passed
    if: github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Verify code jobs actually ran
        id: verify
        run: |
          RUN_ID="${{ github.event.workflow_run.id }}"

          # Skip release build for docs-only changes (slow jobs were skipped)
          REQUIRED_JOBS="validation_test build_android"
          SKIP=false

          for JOB in $REQUIRED_JOBS; do
            CONCLUSION=$(gh api "repos/${{ github.repository }}/actions/runs/${RUN_ID}/jobs" \
              --jq ".jobs[] | select(.name == \"$JOB\") | .conclusion" 2>/dev/null || echo "")
            if [ -z "$CONCLUSION" ] || [ "$CONCLUSION" == "skipped" ]; then
              echo "Job '$JOB' was skipped — docs-only change, skipping release build"
              SKIP=true
              break
            fi
          done

          echo "skip=$SKIP" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Checkout
        if: steps.verify.outputs.skip != 'true'
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Setup Java and Gradle
        if: steps.verify.outputs.skip != 'true'
        uses: ./.github/actions/setup-java-gradle

      - name: Decode Keystore
        if: steps.verify.outputs.skip != 'true'
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > ${{ github.workspace }}/release.keystore

      - name: Write API keys to local.properties
        if: steps.verify.outputs.skip != 'true'
        run: |
          echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" >> local.properties

      - name: Inject google-services.json
        if: steps.verify.outputs.skip != 'true'
        env:
          GOOGLE_SERVICES_JSON_BASE64: ${{ secrets.GOOGLE_SERVICES_JSON_BASE64 }}
        run: |
          # compose-app/google-services.json is committed as a mock (project_id
          # mock-project-id) so local and PR builds compile without Firebase
          # credentials. Release builds overwrite it with the real config; without
          # it the cloud AI chat reports "Firebase is not configured with a valid
          # Gemini API key" at runtime. See bb-fbai-setup in TODO.md.
          if [ -z "$GOOGLE_SERVICES_JSON_BASE64" ]; then
            echo "::warning::GOOGLE_SERVICES_JSON_BASE64 is not set - shipping the mock google-services.json. Cloud AI chat will be unavailable in this build."
            exit 0
          fi

          printf '%s' "$GOOGLE_SERVICES_JSON_BASE64" | base64 --decode > compose-app/google-services.json

          if ! PROJECT_ID=$(jq -er '.project_info.project_id' compose-app/google-services.json); then
            echo "::error::GOOGLE_SERVICES_JSON_BASE64 did not decode to a valid google-services.json."
            exit 1
          fi

          if [ "$PROJECT_ID" = "mock-project-id" ]; then
            echo "::error::GOOGLE_SERVICES_JSON_BASE64 holds the mock config. Upload the real google-services.json from the Firebase console."
            exit 1
          fi

          echo "Injected google-services.json for Firebase project: $PROJECT_ID"

      - name: Build Release Bundle
        if: steps.verify.outputs.skip != 'true'
        run: |
          # Use versionCode 1 — this is a verification build, not a real release.
          # The actual versionCode is assigned by release-android.yml when tagging.
          # Must be >= 1 for Android Gradle Plugin to accept it.
          ./gradlew :compose-app:bundleRelease --stacktrace \
            -PVERSION_CODE=1 \
            -PKEYSTORE_PATH=${{ github.workspace }}/release.keystore \
            -PKEYSTORE_PASSWORD=${{ secrets.KEYSTORE_PASSWORD }} \
            -PKEY_ALIAS=${{ secrets.KEY_ALIAS }} \
            -PKEY_PASSWORD=${{ secrets.KEY_PASSWORD }}

      - name: Upload Bundle Artifact
        if: steps.verify.outputs.skip != 'true'
        uses: actions/upload-artifact@v7
        with:
          name: release-verification-${{ github.event.workflow_run.head_sha }}
          path: compose-app/build/outputs/bundle/release/*.aab
          retention-days: 30

      - name: Cleanup Keystore
        if: always()
        run: rm -f ${{ github.workspace }}/release.keystore
